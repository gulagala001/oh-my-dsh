# 更新记录

## 1.3.0-alpha.6 · 必要更新

摘要统一为简短平铺直叙的范围内实际操作、改动与结果。取消需求复述、范围外进展、未来计划和未完成事项；预处理、合并与全量压缩共用写作规则，字段说明同步。

标明原始范围和仅供参考的材料，避免参考前文或最新进展混入旧摘要。尚未应用的旧规则合并结果会作废；已保存摘要与原始日志不自动改写。本次不修改预处理调用频率。

## 1.3.0-alpha.5 · 普通更新

左上角品牌旁新增圆圈 i，可查看当前版本、最新版本和更新说明。

普通更新显示绿色小点；待安装版本中含有必要修复时显示红色小点。标记在安装更新后解除，不会因查看而消失。支持手动检查、网络失败提示、检查缓存和预发布版本比较；不会自动安装或重启。

## 1.3.0-alpha.4 · 必要更新

修复上下文预处理被消息切碎、原文积压、历史推理成本漏算等问题。引入整窗预处理及文档、图片、附件统一分层，用户原话逐字归档；同步完善压缩统计、有限重试和回查。

包含 `/compact-p`、`/compact-f` 与对应侧栏入口。系统提示词和前置 CoT 保持保护，原始日志与附件原件不删除。

---

自动更新提示读取仓库中的 `release-manifest.json`。这里只记录已发布版本，不以提交标题推测更新等级。
