# 更新记录

## 0.1.7-rc.2.17（发布候选）

- 模型入口改为紧凑的 Codex 风格面板，提供模型搜索、推理滑杆、恢复默认、键盘与触摸操作；补齐粒子、渐变、光晕与减少动态效果适配，不提供 Fast。
- 新增滑杆最后一档 Ultracode；模式独立持久保存，按模型声明映射实际最高推理档，加入完整工作流参考及开启、续、关闭和单回合关键词提醒。
- 扩展原生 Workflow，支持保存脚本、命名调用、单层嵌套、最长成功前缀续跑、独立 worktree、原生 agent preset 和每个子代理的模型／推理选择；沿用宿主 Jobs、权限、取消与子代理生命周期。
- 人类消息末尾的 `+500k` 等指令提供主会话与后代共享的输出目标；后台工作保留发起回合的计量，缺失用量明确处理。
- 隔离工作区保留发起项目的上下文范围；模式状态不向原生日志写入未知事件，支持原生历史读取、进程重启与插件重新启用。

## 0.1.7-rc.2.16

- 修复“需求理解 · OMD UI 增强版”远程安装包在部分 pnpm 环境中缺少 integrity 导致安装失败：先按固定 SHA-256 校验公开发行包，持久保存后交给原生插件管理器安装，保留包管理器校验与兼容性检查。
- 推荐卡片明确原作者为啃轮胎的西狐（WestFox-AwA），OMD 适配作者为 gulagala001；补充原作链接、BSD-3-Clause 许可和非上游官方发行版说明。

## 0.1.7-rc.2.15

- 推荐插件新增独立的“需求理解 · OMD UI 增强版 0.1.0”，默认不安装，手动安装后也默认关闭。
- 固定到已核验的 GitHub Release 安装包；新组件不携带额外 Bash，不改变 OMD 原有提示词优化、记忆、压缩或工具。
- 隔离环境完成实际公开包下载、手动安装、关闭状态和卸载验证。

## 0.1.7-rc.2.14

- 文档新增任务导航与首次使用指南，统一 GitHub 的问题反馈和 PR 入口；Windows 安装命令跟随当前配对版本，文档检查覆盖所有安装目标及本地章节链接。
- 修复上下文操作错误被状态轮询清除，以及切换会话后迟到的操作响应干扰当前状态；过期的资料读取失败不再覆盖新结果。
- 修复会话范围的旧读取覆盖新选择、保存失败原因消失，以及上一会话的待处理保存锁住下一会话选择的问题。
- 全局背景保存冲突时保留草稿，支持读取、比较最新内容并继续合并；仍通过版本校验防止覆盖其他窗口后续的修改。
- 基础组件路径只提交实际编辑的字段，保存响应保留请求期间的新输入。
- 修复源码 checkout 换目录后沿用同一数据目录启动时，仍加载旧 `link:` 插件的问题；保留已有发行包安装来源。
- 修复推荐插件操作失败提示被后台状态刷新清除；失败后可直接重试。
- 修复推荐插件管理服务停止后，尚未提交的卸载仍可能继续执行，以及查询版本期间其他安装操作或管理权限变化未被重新检查的问题；未知操作结果不再误报成功。
- 桌面版本不一致时提示完整退出或重启应用与 Host，不再提供无法完成切换的页面刷新。
- 安装入口明确来源和环境选择；补充排障、Bug 反馈及贡献流程，修正版本徽章、OpenCU 版本和卸载命令。

## 0.1.7-rc.2.13

- 内置 OpenCU 1.1.7：WSL 可通过 `computerUseNativeBinary` 使用手动指定的 Windows 桌面程序。
- 补齐后端平台选择、程序身份及连接校验、Windows 状态与手动维护界面。
- 应用操作说明按实际 Windows 后端装配，覆盖首次加载、回读与重置。
- 新增配置与构建说明、只读连接自检；WSL 真机截图、输入、停止与进程清理仍待验收。

## 0.1.7-rc.2.12

- 修复官方桌面版的容器类名与 Web 版不同，导致会话区壁纸被实色底层遮挡、区域挂载失效。
- 壁纸与高级分区底色通过稳定的布局结构识别侧栏、会话区和工作台，兼容两种构建。
- 修复默认主题未选配色时面板不透明度失效；卸载清理新增的视觉标记。
- 官方桌面回归新增浅深色、三个区域的真实像素对比，直接检查壁纸是否被遮挡。

## 0.1.7-rc.2.11

- 新增高级外观设置：分区颜色、浅深色文字与交互色、字体与字号、行高、圆角、边框、面板模糊和阴影；支持单项恢复、暂时关闭和整体恢复。
- Logo 与浏览器标题可独立保留 DSH 原样或自定义，支持本地 Logo；原生模式释放品牌插槽和浏览器图标，保留宿主会话标题更新。

- 背景新增「整个界面」「仅会话区」「仅左侧栏」，按所选区域尺寸显示，兼容旧背景记录。
- 清除输入区底色及收起工作台残留底色对壁纸的遮挡；保持文字、菜单与弹窗可读。
- 区域选择支持刷新、桌面重启与同源标签页同步，降低透明效果时仍可恢复原设置。
- 修正会话范围选择器的重复边框；仓库一并保留 Euler Neon Fluid 单文件演示。

## 0.1.7-rc.2.10

- 新增 24 套独立配色，总计 28 套；每套提供浅色与深色色板，覆盖自然绿、青蓝、紫粉、暖色和中性色。
- 配色选择器按色系分组并显示总数；保留四套经典配色、导入主题配色及已有选择。
- 新配色不复制布局或字体，继续兼容各主题材质和自定义背景；校验文字、状态和按钮的颜色对比度。

## 0.1.7-rc.2.9

- 推荐插件页面默认显示；可在“设置 → Oh My DSH → 常用 → 显示推荐插件页面”关闭，保存后立即生效并持久化。仅隐藏页面，不卸载插件、不改变自动更新设置。
- 内置 OpenCU 1.1.6：以公开回调接口接收命令输出，兼容丢失 execFile 自定义 promisify 协议的控制台隐藏补丁，修复 Windows 桌面检查和 Chrome 注册表操作收到 undefined 的错误。
- 浏览器、桌面和 Chrome 状态查询独立处理失败，保留正常组件、具体错误和修复入口；不会因一个组件报错让整页显示错误状态。
- 保留主题、配色、背景、记忆、待办、附件和电脑操控设计；本次不实施模块化，也不修改 Logo 或标题行为。

## 0.1.7-rc.2.8

- 主题、配色与背景独立选择；现有默认外观、明暗模式和导入格式保持兼容。
- 从现有主题提取黑白灰、冰蓝、暖砂、晴空蓝配色；改变颜色不替换布局、字体和玻璃材质。
- 新增本地 PNG／JPEG／WebP 背景、模糊、明暗遮罩、面板不透明度与单独恢复默认；图片保存在当前浏览器，不上传。
- 保留跨标签页同步和恢复入口，降低特效时暂时隐藏背景；修复 Codex 长设置页控件被压缩的问题。
- 继续包含上一版实时画面推送崩溃修复，不修改记忆、待办、模型或电脑操作链路。

## 0.1.7-rc.2.7 (2026-09-26)

- 修复电脑／浏览器画面推送结束后继续发送保活消息，触发 `ERR_STREAM_WRITE_AFTER_END` 并导致整个 DSH Host 退出的问题。
- 正常结束、主动断开及同步／异步流错误统一清理定时器、待发画面和观察订阅；断开时尚未完成的订阅也会正确释放，避免重复清理和未捕获异常。
- 禁止宿主 gzip 缓存实时画面推送，保留慢速连接下的最新帧合并和背压控制。
- 修复调整预览尺寸时跳转页面可能卡住旧画面：取消旧截图后，重新请求当前文档，不再保留过期的待处理请求。
- 保留现有记忆、待办、附件、模型、会话、插件兼容与电脑操作功能；本版不修改 Chrome 扩展或原生运行时，无需重载扩展或重新授权。

## 0.1.7-rc.2.6

- “查看发布记录”固定打开完整发布列表，检测到的新版另有说明入口；离线与当前版本状态也不会跳转到旧 tag。
- 标明旧 1.7.4 的历史编号身份并提供最新版入口，避免 GitHub 同日版本排序误导。

## 0.1.7-rc.2.5

- 保留品牌旁的版本图标，在版本面板内增加一键更新、安装进度、失败重试和待重启提示，复用 DSH 原生插件管理器。
- 新版说明链接指向检测到的版本；安装完成与实际运行版本分开显示，更新接口使用宿主认证与来源校验。
- 修复运行状态长短切换时操作数与失败数被挤压换行、导致整行高度跳动的问题。

## 0.1.7-rc.2.4

- 新增两字段插件投稿入口、只读批量队列与一次确认的 AI 处理说明。
- 已核验推荐固定版本安装/更新；未核验旧推荐保留手动操作，不自动更新。
- 状态轮换推荐固定到上游 0.27.0，复用标题共存修复，不增加重复适配补丁。

## 0.1.7-rc.2.3

- 修复 Windows 迁移原生依赖的 CommonJS 默认导出语义，避免 `koffi.load` 不可用导致旧会话迁移失败。
- 保留宽范围 V0 迁移；完整包含 0.1.7-rc.2.2、空用户占位及官方 Agent 预设槽位修复。
- 作为已发布 0.1.7-rc.2.2 的后续补丁发布，不改写旧标签或替换旧版本安装包。

## 0.1.7-rc.2.2

- 修复官方 Agent 预设选择器和会话标题标签消失（#13）：恢复会话槽位先于服务发布的挂载顺序，继续使用官方选择器、只读标签及代码工作工具开关。

- 修复上下文压缩和运行状态清理生成空用户消息，导致后续请求持续报 `400: user message must have content`。发送前自动修复旧占位，兼容预设切换与子会话，保留原始日志和回查资料。

- 修复思考时 Web 标签页在 Oh My DSH 与 DeepSeek Harness 之间快速切换（#12），不再通过标题观察器反复改写。
- 敏感 API 统一接入宿主认证与来源校验；中文 JSON 按字节收齐后解码，增加正文限制，未知会话查询不创建状态。
- 长会话检查复用位置索引，释放已结束会话的无主缓存；坏档案局部报告，重复状态保存不再反复刷盘。
- 项目摘要按剩余容量注入，未注入记录仍可回查；摘要目录增加分页，组件页首次失败可以看到错误并重试。
- 推荐插件区分操作中临时警告与最终失败；固定本地插件包验证原生安装链路，第三方在线兼容检查独立运行。

- 修复桌面旧 V0 会话历史加载失败：兼容早期权限来源、TriSoul 推理与任务附加字段、子会话描述及压缩记录；保留原始日志，严格验证新副本，异常旧记录不阻断其他会话迁移。

## 0.1.7-rc.2.1

- 修复 macOS 桌面端左上角版本信息入口消失，兼容新版宿主的品牌容器及不同安装包的样式类名。
- 版本号跟随完整 DSH 宿主版本，插件补丁在末尾递增；本版对应 DSH 0.1.7-rc.2。修正新旧编号的更新判断，旧 1.x 用户首次切换需按新 tag 手动更新。

[安装与验证范围](docs/release-0.1.7-rc.2.1.md)。

## 1.7.4

- 内置 OpenCU 1.1.4，自动迁移本产品的旧 Chrome 连接并保留已加载的扩展目录；防止旧实例启动时抢回连接，失败时恢复旧注册与文件。
- 修复桌面 Electron 本机连接程序启动失败；扩展 0.1.6 支持迁移后自动断开旧连接、释放按键并重连，尊重用户主动断开。
- 修复三套皮肤收起右栏后残留背景、模糊、阴影和边框，覆盖辅助显示设置（#8）。
- 合并 PR #10，兼容 npm 11 数组与 npm 12 对象形式的打包结果。

[安装与验证范围](docs/release-1.7.4.md)。

## 1.7.3

- 适配 DSH 0.1.7-rc.2 与官方桌面版，内置 OpenCU 1.1.3；同步 Tools、Jobs、Shell、Conversation 和 Chat 的上游变化。
- 上下文摘要替换与全量压缩保留动态工具声明，保持工具启停信息及重启回放一致。
- 修复首次热安装时文件上传服务遗留注册；保留有效服务，不重试无关启动错误。
- 修复 Chrome 扩展刷新状态时忽略停止／断开点击的竞态。
- 四套主题融合新版快捷键、模式入口与额度提示，修正窄屏预设卡片提示气泡溢出。
- 桌面使用独立 profile，通过应用内插件管理安装；安装、更新或启停后完整重启应用，界面提示完成切换。具体平台验证和限制见 [1.7.3 发布说明](docs/release-1.7.3.md)。

## 1.7.2

- 适配 DSH 0.1.7-rc.1，内置 OpenCU 1.1.2。
- 融合四档工作步骤、准备/执行/结果状态和图片预览，保留 OMD 增强与旧显示习惯。
- 修正四套主题收起侧栏的图标对齐，补齐推荐插件的版本兼容诊断。
- 安装与升级需同时配对新版宿主；alpha.2 用户保留 OMD 1.7.1。

## 1.7.1 — 2026-09-23 · 正式版

- 适配 DSH 0.1.7-alpha.2，内置 OpenCU 1.1.1，保留上下文、任务验证、BT、预算、PTC 和四套皮肤。
- 融合原生滚动跟随、历史定位、统一代码卡片和多行排队消息编辑；保留完成汇总、展开阅读状态与紧凑间距。
- 后台任务默认不再受连续三次唤醒限制；显式设置上限仍生效，保留插话唤醒和输出预览。
- 适配 MCP 文本与图片内容投影及统一 token 保留预算，承接宿主重连、模块加载和 PowerShell 改进。
- 安装入口分为自己安装和 AI 安装；AI 手册覆盖环境识别、数据备份、版本配对、升级与回退。

[安装与升级说明](docs/release-1.7.1.md)。

## 1.7.0 — 2026-09-23 · 正式版

- 正式融合 DSH 0.1.7-alpha.1，内置 OpenCU 1.1.0；包含 1.7.0-rc.1 的全部适配。
- 修复展开后的“思考”和动作行间距，缩小标题下方留白，保留正文段落间隔。
- 同步正式版安装入口、发布记录和发行包。[安装与升级说明](docs/release-1.7.0.md)。

## 1.7.0-rc.1 — 2026-09-23 · 预发布

- 融合新版原生工作详情、设置、预设、Jobs 和 V4 会话；保留上下文、任务验证、后台双时限、PTC 与四套皮肤。
- 工具已结束时立即显示完成汇总，保留展开阅读状态；移除旧分组执行器及对应过时断言。
- 迁移旧配置与上下文引用，保留原日志和恢复记录；修复 Computer Use 重置提示、双安装挂载顺序与整包卸载。
- 子路径 API、提示词优化和主题布局同步适配；Windows 真机本轮未复验。[验证与升级说明](docs/release-1.7.0-rc.1.md)。

## 1.6.1 — 2026-09-21 · 正式版

- 执行中的操作抽屉持续显示最新动作，优先复用调用说明，否则显示工具名称和关键参数；工具返回后等待模型继续时仍保留，整轮结束后恢复简写汇总。
- Job、list_agents 等通用工具调用，以及普通上下文、Todo／运行状态记录统一收进抽屉；上下文压缩保持独立显示。
- 展开可查看原始参数、结果、失败和停止状态，保留已打开的详情；刷新后按实际回合状态恢复展示。
- 操作抽屉滚动到边缘后可继续滚动外层对话，Computer Use 悬浮预览支持从内容区域拖动。
- 上下文后台短重试耗尽后，可在主会话请求恢复成功时自动重试一次；保留退避、并发限制和手动重试。

兼容 DSH 0.1.6-alpha.2，插件 ID、预设及数据目录保持不变。[验证与升级说明](docs/release-1.6.1.md)。

## 0.1.6 — 2026-09-21

### 上下文恢复补丁（版本号不变）

- 跟随主会话模型时，预处理与上下文替换判断因临时服务错误耗尽短重试后，在主会话请求成功时自动尝试一次；保留退避、并发限制与手动重试，兼容已保存的失败状态。
- 工作上下文明确展示等待主会话恢复、等待自动重试和需要手动重试，避免错误显示为等待新事件。

### 会话预算补丁（版本号不变）

- 新增默认关闭的会话预算提示，支持 `/预算` 与 `/budget` 设置总 token、模型轮次、执行时间；未设置显示无限制。
- 预算默认沿用原状态节点；新增默认关闭的“每轮注入预算”子开关，开启后才显示每轮、每 3／5／10 轮和自定义间隔。额外注入只含预算，压缩排除与旧快照清理规则不变。
- 超过 100% 继续如实展示，不强制停止；支持查看、修改、重置和关闭。
- 预算按会话保存，执行时间排除等待用户、空闲和停机；累计已返回 token，包含缓存输入、后台与子代理用量。
- 修正状态实际回合与步数，限制操作详情布局并去重压缩注入记录，改善 Codex 主题用户消息对比度。

### 首次发行

- 融合 Prompt Optimizer 的 MIT 历史版本优化模板，复用 DSH 当前模型；默认显示输入框星星，提供轻润色、结构化、步骤规划、版本回退及补充要求后的连续修改。
- 新增默认关闭的自动轻润色：点亮星星后，每次发送前按最低档润色并自动交回宿主发送；保留附件、引用和排队/接管语义。取消、失败或编辑冲突时保留草稿，斜杠命令直接交由宿主处理。

- 新增可选原生皮肤系统，保留四套从零制作的完整主题：Codex Desktop、iOS Liquid Glass、Claude CLI、Google Material；独立布局覆盖侧栏、对话、设置、工作台及窄屏，移除早期五套视觉皮肤。
- Codex 顶栏贯通对话与工作台，右侧栏开关固定在最右端，默认宽度 360px；拖动偏好单独保存，退出皮肤恢复原布局宽度。
- 输入区按实际高度约束在对话列底部，防止展开／折叠记录时向上跳位；提示词优化的展开按钮可固定已悬浮打开的面板。
- 优化操作记录分组、过程折叠与对话排版；历史加载可选 50、200、500 条，原文和已展开状态保留。
- 统一上下文设置校验，拒绝无效类型和已退役参数；压缩、替换与历史记录通过批次标识关联。
- 清理已退役的自动记忆、探针、状态提炼执行器及未装配提示词副本；旧资料继续只读访问，当前 ContextPipeline、Todo 与验证账本不变。
- 同步 OpenCU 操作分组修正与第三方字体许可，发行包排除本地数据和内部交接文档。

## 0.1.6-alpha.2.5 — 2026-09-20

- 推荐插件加入 [Jevify](https://github.com/gulagala001/jevify)，独立发行，用户按需安装，不作为 OMD 内置依赖。
- 推荐插件支持 GitHub Release 安装包的安装、更新与卸载。

## 0.1.6-alpha.2.4 — 2026-09-20

- 明确 Todo 创建时顶层摘录与任务锚点均须填写 `from/to`，短引用允许两者相同；执行与校验规则不变。
- 新增推荐插件设置页，支持标准插件包的一键安装、更新、卸载及默认关闭的自动更新；展示需要手动配置的插件说明。
- 修复通用工具卡片开头时后续文件操作被隐藏且无法展开的问题；通用卡片独立显示，后续操作保留分组入口。

## 0.1.6-alpha.2.3 — 2026-09-19 · 必要更新

- 修正运行状态注入频率：保留真实新输入、开启状态、Todo 投递、压缩刷新与提示块缺失补回。
- 没有 Todo 时仍能提供状态；后台任务、上下文内部变化、自动新回合和模型切换不再独立触发。
- 压缩刷新后不重复追加，保留任务账本、验证证据、历史原文和按需状态查询。
- 后台优化与 CoT 前置默认开启，运行状态与 CFR 默认关闭；已保存设置不重置。

## 0.1.6-alpha.2.2 — 2026-09-19

- 新增默认关闭的运行状态和后台任务优化开关；[使用与行为说明](docs/runtime-state-background.md)。
- 状态与 Todo 合并投递，保留压缩、回放和原文回查；不虚构任务额度。
- 长命令短等后交给原任务注册表，保留硬超时；结果自动预览，等待可响应插话。PTC 内部 await 保持最终结果语义。
- Todo 支持 `pause_turn`，保留未完成任务与证据；重复收尾提醒按回合去重。
- 基础组件统一设置，CodeGraph 自动索引，Computer Use 自动准备。内置 OpenCU 1.0.4。
- 配套 DSH 组件使用同一安装的宿主依赖，保留原预设及卸载恢复。
- 输入框直接随 OMD 打包，支持 GitHub 安装；修正 Windows 路径、发行快照换行和 CodeGraph 缓存目录识别。

- 新增独立 `omd-ptc` 预设：保留 OMD 的提示词、记忆、压缩、任务验证、CodeGraph、电脑操控与子代理，使用 `run_code` 调用工具；原 OMD 与宿主 PTC 预设保留。

- 修正 PTC 与混合调用模式的主提示词、SDK 返回值说明及计划提交方式；区分外层程序与 Computer Use 的独立运行环境。

- 内置 colbymchenry/codegraph 1.6.0：随插件安装平台运行时，通过 MCP 提供代码探索；支持按会话工作目录建立索引、跨项目查询、文件自动同步及空闲连接释放，关闭上游遥测。

- 新增“实验性功能”设置页，集中管理 CoT 前置及字符上限、任务约束前置（CFR）。
- CFR 默认关闭；开启后在主系统提示词与 todo 工具提示词中加入约束提取、共同限制推导和执行核对规则，保存后从下一次请求生效。
- 已前置的推理不再从原位置重复发送给模型；保留正文、工具往返与可回查的原始日志。

## 0.1.6-alpha.2.1 — 2026-09-18

已公开发布为 alpha 预发布版。后续验证仍有一项 OpenCU Windows 测试超时，详见[已知限制](docs/release-0.1.6-alpha.2.1.md#发布状态与已知限制)；不将其记为全部通过。

- 修复 DSH 工具调度模块身份冲突和热卸载审核竞争；工具执行、失败语义和权限不变，不重复执行操作。
- 修复 Windows 新目标的首次导航竞争与导航中断后的预览恢复；附带官方源码打包输出目录校验补丁及适用范围说明。

本版适配 DSH 0.1.6-alpha.2，并优化前后端运行开销；现有功能、提示词与记忆设计保持不变。

### 主要变化

- 接入 DSH 新版会话状态、用量面板与插件热卸载；多会话的用量展开状态不再相互影响。
- 内置 OpenCU 1.0.3：增加同会话官方浏览器预览入口，保留现有网页、桌面、截图、批注、接管、恢复与标签分组能力。
- 保留一键应用中枢方案与手动选段切换模式的区别；新增保留、简要及混合方案的回归，防止优化误改压缩语义。
- 统计条改为轻量响应；归档监控不再载入完整会话，减少重复解析、无变化写盘与重复日志扫描。
- 合并 CU 顶栏和侧栏的状态请求，处理迟到响应与卸载清理；保留独立预览窗口所需的后台状态更新。
- 长会话操作摘要改为单次扫描建立索引；前端构建压缩语法与空白，保留标识名称。
- 修复共享状态更新后新标签页地址栏的焦点时序；锁定 fflate 0.8.3 补丁，修复项目依赖链中的 ZIP 解析漏洞。

### 升级说明

先备份原数据目录，停止服务，按原安装方式升级宿主到 DSH 0.1.6-alpha.2，再安装本版插件。保留原来的 DSH_HOME、profile、模型配置与凭据。

本版开始使用跟随 DSH 的版本编号。历史 1.3.0-alpha.* 属于旧版本，旧客户端可能无法识别这次编号切换，首次请直接安装 v0.1.6-alpha.2.1；新版本已处理后续比较。

保存的频率、空闲开关、身份、模型、原文、Todo 与上下文档案不重置。默认适中档沿用此前已调整的 48 条事件、48 条窗口、3 段摘要、60 秒中枢间隔与 30 步替换间隔。

官方浏览器入口打开的是独立网页预览，不替换 CU 控制的浏览器，也不保证共享登录状态。网站的嵌入限制仍然适用。


## 1.3.0-alpha.10 · 普通更新

重新校准处理频率：适中作为默认档，使用 48 条新事件、48 条窗口、3 段新摘要、中枢最短间隔 60 秒、替换最短间隔 30 步。频繁档调整为 32／32／2／30 秒／20 步，较少档调整为 64／64／4／120 秒／40 步。

宿主配置、处理管线与设置界面共用频率定义，避免默认值不一致。升级保留已保存参数，原较少档会显示为适中；自定义参数及模型、Trace、空闲设置不变。

## 1.3.0-alpha.9 · 必要更新

取消用户发言直接触发中枢，以及新发言导致待执行方案取消、生成结果丢弃并再次处理的行为。自动处理中枢按新增摘要数量和配置间隔运行，同一预处理批次完成后统一评估；手动触发、可选空闲处理及有限失败重试保留。记录本身发生变化时仍检查来源，避免应用过期内容。

关闭中枢合并，仅保留“保留／详细／简要”选择。尚未执行的合并方案自动取消，已有档案、原始日志和手动压缩能力保留。

## 1.3.0-alpha.8 · 必要更新

预处理跳过 todo 快照及任务工具正文，但不因此切断窗口。真正应用压缩时，在同一可恢复事务中清理旧 todo 快照，并从任务账本重新注入最新版；放在前置 CoT 后，无 CoT 时放在系统前缀后。平时任务更新仍按原规则追加，内容不变不重复注入。

用户原话作为逐字、按序的文本附件，与文档和图片同属详细资料；仅摘要模式保留回查索引而不携带原话全文。原始日志与任务账本不删除。压缩生成失败或没有收益时不清理旧 todo；写入中断可恢复同一事务。

## 1.3.0-alpha.7 · 必要更新

后续预处理窗口只有在新的实际消息达到窗口数量，或文本量达到续跑门槛（默认约 8,000 tokens）时才继续。参考前文、旧摘要、附件二进制大小不参与门槛。零碎剩余保留在积压中；失败重试仅重试一窗，不重新获得整批额度。

中枢对照请求开始时的记录版本与内容，丢弃生成期间过期的回答，不误记模型失败、不强制连续重试、不覆盖更新的决定。新记录按配置频率触发，用户新指令和真实调用失败保留各自触发行为；普通定时任务不会覆盖用户触发的判断，已拒绝的同一方案不会凭旧计数继续循环。已排队决定在应用前也检查内容快照。侧栏显示暂缓续跑和过期决定状态。

包含上一版本仅范围内事实的简短摘要规则；原日志和资料档案不改写。

## 1.3.0-alpha.6 · 必要更新

摘要统一为简短平铺直叙的范围内实际操作、改动与结果。取消需求复述、范围外进展、未来计划和未完成事项；预处理、合并与全量压缩共用写作规则，字段说明同步。

标明原始范围和仅供参考的材料，避免参考前文或最新进展混入旧摘要。尚未应用的旧规则合并结果会作废；已保存摘要与原始日志不自动改写。本次不修改预处理调用频率。

## 1.3.0-alpha.5 · 普通更新

左上角品牌旁新增圆圈 i，可查看当前版本、最新版本和更新说明。

普通更新显示绿色小点；待安装版本中含有必要修复时显示红色小点。标记在安装更新后解除，不会因查看而消失。支持手动检查、网络失败提示、检查缓存和预发布版本比较；不会自动安装或重启。

## 1.3.0-alpha.4 · 必要更新

修复上下文预处理被消息切碎、原文积压、历史推理成本漏算等问题。引入整窗预处理及文档、图片、附件统一分层，用户原话逐字归档；同步完善压缩统计、有限重试和回查。

包含 `/compact-p`、`/compact-f` 与对应侧栏入口。系统提示词和前置 CoT 保持保护，原始日志与附件原件不删除。

---

自动更新提示读取仓库中的 `release-manifest.json`。这里只记录已发布版本，不以提交标题推测更新等级。
