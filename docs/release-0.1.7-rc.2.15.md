# OMD 0.1.7-rc.2.15

本版配套 DSH 0.1.7-rc.2，继续内置 OpenCU 1.1.7。

推荐插件新增 **需求理解 · OMD UI 增强版 0.1.0**，源码与安装包独立发布在 [omd-prompt-optimizer](https://github.com/gulagala001/omd-prompt-optimizer)。它复用 WestFox-AwA 的 dsh-prompt-optimizer 0.7.4 解释与校验引擎，采用 OMD 风格界面与独立启停逻辑。

**不会自动安装。** 只有在“设置 → 推荐插件”点击安装才获取插件。安装后还需在“设置 → 需求理解”主动启用，默认采用审查模式。开启自动更新也不会安装尚未安装的推荐项。

OMD 本体本次只增加推荐信息，不把解释器、Bash 或额外自动调用加入内置组件。已有提示词优化、记忆、压缩、前置推理、待办和电脑操作链路不变。

## 验证

增强版使用隔离的 DSH Web profile 和模拟模型验证接线。没有以此宣称真实模型能力提升、一定省 token，或已完成所有原生桌面平台验证。已发送的随行内容仍作为历史保留；关闭只停止新的处理。

### 本次实际结果

- OMD 文档、推荐管理器、投稿元数据与版本检查：33/33 通过。
- 原有提示词优化与推荐界面/原生插件管理回归：22 项通过，1 项与本次无关的第三方在线安装测试按默认策略跳过。
- 通过推荐页面实际下载增强版公开 Release、安装、确认默认关闭、卸载及 API 释放，全流程通过。
- 增强版独立仓库：14 项单元测试、53 项上游引擎测试和真实 Web 隔离集成通过。
- 增强版 GitHub Actions 在 Ubuntu、Windows、macOS 上的构建和单元/上游测试通过；跨平台 CI 不等于原生桌面实测。
