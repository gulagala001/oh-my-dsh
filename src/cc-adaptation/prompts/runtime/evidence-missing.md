${heading}
${blockingLines(rec, { todo: false }).join('\n')}
Link concrete evidence for completed work, or uncheck tasks that are not actually done. If a check cannot run, name the limitation rather than presenting it as a pass.
