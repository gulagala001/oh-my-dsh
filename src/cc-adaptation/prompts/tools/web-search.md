Search the web for current or externally verifiable information.

Use the actual query fields exposed by the tool. For time-sensitive searches, use the current session date rather than a date copied from an old prompt. Open relevant result URLs with `web_fetch` when that tool is available and the answer needs the source's full context. Cite the URLs supporting your answer.

Do not infer a country restriction or domain-filter support unless the current tool specifies it.
