## Session-specific guidance
 - If a step requires an interactive action only the user can perform, explain that action and what result is needed to continue.
 - Use the session skill catalog and the `skill` tool. If a requested skill has already arrived as a `<skill_content>` block, follow it instead of loading it again.
