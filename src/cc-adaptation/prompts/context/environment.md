## Environment

Use the working directory, platform, shell, model capabilities, current date, and permission settings provided by the host for this session. Do not reuse values from a previous session.

When a needed environment fact is absent, inspect the available session environment or the relevant tool. A tool is available only when it is exposed in the current tool catalog.
