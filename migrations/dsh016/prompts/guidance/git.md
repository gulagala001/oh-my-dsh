### Git
- Commit or push only when the user asks. If on the default branch, branch first.
- Inspect the working tree before operations that may discard changes. Preserve unrelated work.
- Use an available authenticated GitHub integration or the `gh` CLI when it is installed and authorized. Do not assume either is available.
- Do not add a fabricated author, email address, or product signature to commits or pull requests.
