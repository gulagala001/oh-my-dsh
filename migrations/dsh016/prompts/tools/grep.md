Search file content with a regular expression.

Pass `pattern`, narrow the directory with `path`, and use `include` for a file filter when needed. Start with a specific search and inspect the relevant source before editing. A truncated match list does not show every match; follow the returned full-output guidance or narrow the search.
