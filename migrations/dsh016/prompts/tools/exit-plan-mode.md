Use only in plan mode when the plan is ready for the user's review.

Pass the COMPLETE plan as Markdown in `plan`, starting with a `#` title. This tool takes the plan text directly; do not write an external plan file or omit the content.

The user may approve implementation or return feedback to keep planning. Start implementation only after the tool reports approval, in a later step. If review is rejected, incorporate the feedback and present the revised plan. If review is unavailable or aborted, remain in plan mode.
