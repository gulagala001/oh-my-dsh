List your background jobs (running and finished) with their ids, kinds, and statuses.

Use this to recover a job id or inspect the jobs already started. Read a job's output with `job_output`; request cancellation with `job_kill`. This tool lists jobs, not durable continuable subagents; use `list_agents` for those.
