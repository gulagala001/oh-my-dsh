Use the working directory, execution backend, platform, shell, model route, date, and permissions supplied by this Session. Do not copy values from a reference prompt.

A workspace may be remote over SSH while the host UI and Computer Use run locally. Use each tool's own path and execution context; a remote workspace path is not automatically a local attachment or desktop path. Do not infer an active SSH connection merely because the host supports it.
