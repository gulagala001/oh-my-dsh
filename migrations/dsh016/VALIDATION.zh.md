# 验证记录与边界

## 本次已执行

候选包 27 项离线测试重新通过，0 项失败；`scripts/migrate-dsh016.mjs --help` 正常运行，默认从仓库 `migrations/dsh016` 加载，不需要聊天附件。

已将上传的 95 个实现、测试和 Markdown 文件计算 Git 树校验值，与 GitHub 已写入树比较，结果一致。JSON 元数据增加结尾换行，bindings.json 只压缩排版；没有重写候选提示词。全部 87 个 Markdown 模块的字节保持不变。

测试对象为候选纯组装函数、源代码替换函数以及临时 Git 目录中的文件回滚。组装对象、工具定义和多数迁移源使用离线夹具；状态文件使用已审计原版源码。SDK 测试使用注入的测试渲染函数，不是新安装的 DSH 渲染器。

覆盖：主 Agent 作用域；其他 Agent／后台不变；参数与上下文不变；原文不匹配保持原生；reserved run_code 不覆盖；SDK 输出契约；完整 prompt 冲突拒绝；版本和生命周期替换；状态头单句补丁；重复安装拒绝；回滚校验、后续工作保护、损坏备份拒绝和显式应用开关。

## 未执行

- DSH 0.1.6-alpha.1 依赖安装、项目实际构建及完整运行测试。
- 实际 Cordis Loader 的加载与释放。
- 新版公共 SDK 渲染器的真实导入与执行。
- 用户 profile 启动、模型连接、真实请求导出、CU 真机操作。
- 正式环境切换及服务回滚。

不能用离线夹具测试推出上述项目成功。仓库推送、源码迁移、运行环境部署是三件不同的事。
